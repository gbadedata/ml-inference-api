apiVersion: v1
kind: Service
metadata:
  name: ml-inference-api-service
spec:
  selector:
    app: ml-inference-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP